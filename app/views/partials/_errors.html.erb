<% if item.errors.count > 0%>
	<div class="errorExplanation">
	  <h2><%= pluralize(item.errors.count, "error") %> prohibited this <%=item.class.to_s.downcase%> from being saved:</h2>
	  <ul>
	  <% item.errors.full_messages.each do |msg| %>
	    <li><%= msg %></li>
	  <% end %>
	  </ul>
	</div>
<% end %>